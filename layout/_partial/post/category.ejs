<!-- TO DO -->
<% if (post.categories && post.categories.length){ %>
  <span class="separator"> &nbsp;&nbsp; </span>
  <span class="article-category"><%= __('sorted_in') %>
    <%- list_categories(post.categories, {
      show_count: false,
      class: 'article-category',
      style: 'none',
      separator: ', '
    }) %>
  </span>
<% } %>